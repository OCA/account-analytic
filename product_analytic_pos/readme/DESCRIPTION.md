This module applies analytic distribution models defined on products or
product categories to uninvoiced POS sales when the POS session is closed.

If several POS order lines resolve to different analytic distributions, the
closing entry is split so each generated account move line keeps its own
analytic distribution.
