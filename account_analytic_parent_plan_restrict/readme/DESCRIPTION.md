This module extends the analytic account hierarchy introduced by the
``account_analytic_parent`` module. It forces every account in a parent/child
tree to belong to the same analytic plan, so a hierarchy can never belong to
more than one plan.

