This module allows you to configure analytic distribution models on products and on product categories.
When you select the product in an invoice line, it will check if this
product has analytic distribution models configured; if it
doesn't find any, it checks if the category of the product has; if some analytic distribution model is found, it will
be set by default on the invoice line.
