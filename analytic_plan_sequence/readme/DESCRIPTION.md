This module adds a sequence field to analytic plans and uses it to control their
display order in the `analytic_distribution`. When a plan’s sequence changes,
the list is re-sorted accordingly.

Odoo 17.0 includes this functionality natively. Therefore, this module does not
need to be migrated to downstream versions.
