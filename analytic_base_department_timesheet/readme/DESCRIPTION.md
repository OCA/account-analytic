Glue module between *Analytic Base Department* and *HR Timesheet*.

Extends ``timesheets.analysis.report`` with the ``account_department_id`` field
so that the Timesheet Analysis report supports filtering and grouping by
Account Department when both modules are installed.
